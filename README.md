dill - Debug Information Logging Layer
=====

libdill - A library contains both dill client and server functionalities.

dlv - Dill Log Viewer. 
      A GUI interface for viewing log on a dill server (built-in), which is based on wxWidgets
